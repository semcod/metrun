🔍 Inspecting: demo.py

🔥 METRUN PERFORMANCE REPORT
=============================

🟡 handler
   → time:      0.0022s  (100.0%)
   → calls:     1
   → score:     10.8
   → diagnosis: 🐢 slow execution

🟡 slow_query
   → time:      0.0014s  (62.1%)
   → calls:     100
   → score:     8.22
   → diagnosis: 🐢 slow execution
   → called by: handler

── Dependency Graph ──────────────────────────
   handler  →  slow_query

── Critical Path ─────────────────────────────
🧨 Critical Path  (depth=2, hottest leaf: 0.0014s)

  handler  [0.0022s, 1 calls]
    └─ slow_query  [0.0014s, 100 calls]   ← 🔥 hottest leaf (0.0014s)

── Fix Suggestions ───────────────────────────
  💡 Fix suggestions for: handler
     1. Profile deeper with cProfile + snakeviz [cProfile / snakeviz]
        The function is slow but the root cause is not yet clear.  Use cProfile to drill into sub-call timings and snakeviz for an interactive flame view.
           import cProfile
           cProfile.run('my_func()', 'profile.prof')
           # then: snakeviz profile.prof

     2. Check algorithmic complexity
        A single slow call often hides an O(n²) or O(n³) algorithm.  Review loops, sorting, and lookups; switch lists to sets/dicts for O(1) membership tests.
           # O(n) lookup — use a set:
           lookup = set(items)
           if value in lookup: ...

     3. Cache pure function results [joblib]
        If the slow function always returns the same result for the same inputs, wrap it with lru_cache or joblib.Memory.
           from joblib import Memory
           memory = Memory('/tmp/cache')
           
           @memory.cache
           def slow_pure_func(x): ...

     4. Profile memory with Scalene [scalene]
        Scalene provides CPU, GPU and memory profiling at line-level precision — it can reveal hidden allocation costs that slow execution.
           # pip install scalene
           # python -m scalene my_script.py

     5. Visualise with VizTracer [viztracer]
        VizTracer records a full execution trace with microsecond precision and renders an interactive HTML flamegraph — great for understanding nested call patterns.
           from viztracer import VizTracer
           
           with VizTracer(output_file='trace.json'):
               my_func()
           # then: vizviewer trace.json

  💡 Fix suggestions for: slow_query
     1. Profile deeper with cProfile + snakeviz [cProfile / snakeviz]
        The function is slow but the root cause is not yet clear.  Use cProfile to drill into sub-call timings and snakeviz for an interactive flame view.
           import cProfile
           cProfile.run('my_func()', 'profile.prof')
           # then: snakeviz profile.prof

     2. Check algorithmic complexity
        A single slow call often hides an O(n²) or O(n³) algorithm.  Review loops, sorting, and lookups; switch lists to sets/dicts for O(1) membership tests.
           # O(n) lookup — use a set:
           lookup = set(items)
           if value in lookup: ...

     3. Cache pure function results [joblib]
        If the slow function always returns the same result for the same inputs, wrap it with lru_cache or joblib.Memory.
           from joblib import Memory
           memory = Memory('/tmp/cache')
           
           @memory.cache
           def slow_pure_func(x): ...

     4. Profile memory with Scalene [scalene]
        Scalene provides CPU, GPU and memory profiling at line-level precision — it can reveal hidden allocation costs that slow execution.
           # pip install scalene
           # python -m scalene my_script.py

     5. Visualise with VizTracer [viztracer]
        VizTracer records a full execution trace with microsecond precision and renders an interactive HTML flamegraph — great for understanding nested call patterns.
           from viztracer import VizTracer
           
           with VizTracer(output_file='trace.json'):
               my_func()
           # then: vizviewer trace.json

── Summary ───────────────────────────────────
   Top bottleneck : handler
   Score          : 10.8
   Diagnosis      : 🐢 slow execution

